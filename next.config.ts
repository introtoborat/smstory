import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable the X-Powered-By header to avoid leaking tech stack info
  poweredByHeader: false,

  // Compress responses (gzip/brotli) for better performance
  compress: true,

  // Security headers
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          {
            key: "X-Content-Type-Options",
            value: "nosniff",
          },
          {
            key: "X-Frame-Options",
            value: "DENY",
          },
          {
            key: "X-XSS-Protection",
            value: "1; mode=block",
          },
          {
            key: "Referrer-Policy",
            value: "strict-origin-when-cross-origin",
          },
          {
            key: "Permissions-Policy",
            value:
              "camera=(), microphone=(), geolocation=(), interest-cohort=()",
          },
          {
            key: "Strict-Transport-Security",
            value: "max-age=63072000; includeSubDomains; preload",
          },
          {
            key: "Content-Security-Policy",
            value: [
              "default-src 'self'",
              "script-src 'self' 'unsafe-eval' 'unsafe-inline'",
              "style-src 'self' 'unsafe-inline'",
              "img-src 'self' data: blob: https://res.cloudinary.com",
              "font-src 'self'",
              "connect-src 'self'",
              "frame-ancestors 'none'",
              "base-uri 'self'",
              "form-action 'self'",
            ].join("; "),
          },
        ],
      },
    ];
  },

  // Image optimization (allow external sources if needed)
  images: {
    formats: ["image/avif", "image/webp"],
    remotePatterns: [
      {
        protocol: "https",
        hostname: "res.cloudinary.com",
      },
    ],
  },

  // Redirects for common patterns
  async redirects() {
    return [
      {
        source: "/dashboard",
        destination: "/stories",
        permanent: true,
      },
    ];
  },

  // Production build optimizations
  productionBrowserSourceMaps: false,

  // Experimental features (stable in Next.js 16)
  experimental: {
    // Optimize CSS for production
    optimizeCss: true,
  },
};

export default nextConfig;
